<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit</title>
</head>
<body>
    <form action="/product/<%=fdproduct._id %>?_method=PUT" method="POST">
        <label for="name" >Name</label>
        <input type="text" name="name" id="name" placeholder="Name" value="<%=fdproduct.name  %>">
        <label for="price" >Price(unit)</label>
        <input type="text" name="price" id="price" placeholder="Price" value="<%=fdproduct.price %>">
        <label for="category" >Category</label>
        <select name="category" id="category">
            <option value="fruits">Fruit</option>
            <option value="vegetable">vegetable</option>
            <option value="diary">diary</option>
        </select>
        <button>Submit</button>
    </form>
    <a href="/product/<%=fdproduct._id %>">Cancel</a>
</body>
</html>